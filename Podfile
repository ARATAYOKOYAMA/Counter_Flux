# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Counter_Flux' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Counter_Flux
  pod 'RxSwift'
  pod 'RxCocoa'

  target 'Counter_FluxTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Counter_FluxUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
